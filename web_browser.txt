import webbrowser

from speech import speak

def Webbrowser(data):
    data = data.split(" ")
    ask = data[1]
    speak("Wait a minute. I'm searching for a " + ask)
    webbrowser.open('https://google.com?#q=' + ask, new=2)